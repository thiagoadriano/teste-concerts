const { resolve } = require('path');

module.exports = {
  config: resolve(__dirname, 'api', 'database', 'dbEnvConfig.js'),
  'models-path': resolve(__dirname, 'api', 'models'),
  'migrations-path': resolve(__dirname, 'api', 'database', 'migrations'),
  'seeders-path': resolve(__dirname, 'api', 'database', 'seeds'),
};
